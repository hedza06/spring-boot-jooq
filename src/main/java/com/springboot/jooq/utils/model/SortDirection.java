package com.springboot.jooq.utils.model;

import org.springframework.context.annotation.Description;

@Description(value = "Enumeration that represents sort direction.")
public enum SortDirection
{
	ASC, DESC
}
